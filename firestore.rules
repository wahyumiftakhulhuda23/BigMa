rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to userAppData documents for multi-device sync
    match /userAppData/{document} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
